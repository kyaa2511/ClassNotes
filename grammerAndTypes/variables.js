/*
-Variables arenamed containers for storing data values 
-We name the variables so that we can refer to them and the values they hold at a later time
*/

let a =2;

/*
(1)     (2)  (3)  (4)
let      b    =    2

1. JavaScrupt keyword to demote the creation of a variable
2. The name of the variable, in which the developer refers to in order to grab  the value stored
3. Assignment operator
4. The initial value
*/

// The declaration is to the left of the assignment operator(=)
//This is where the variable is created, by urilizing a designated JavaScript keyword(var, let, const) and giving it a name

let s

//The initialization is to the right side of the assignment operator(=)
//This is where the developer sets the valye for the declared variable
//      (1)
let x = 10;
//(1) Initialization

let y;
console.log("Declaration:", y);

y = 10;
console.log("Initialization 1:", y);

y = 35;
console.log("Initialization 2:", y);

